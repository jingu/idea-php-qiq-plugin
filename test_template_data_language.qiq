<!DOCTYPE html>
<html lang="ja">
<head>
    <meta charset="UTF-8">
    <title>{{= $title }}</title>
</head>
<body>
    <h1>{{h $heading }}</h1>

    <!-- PHP injection test -->
    <?php
    $user = getCurrentUser();
    echo "Current user: " . $user->getName();
    ?>

    <div class="content">
        {{= $content }}
    </div>

    <ul>
        {{ foreach ($items as $item): }}
            <li>{{h $item->name }}</li>
        {{ endforeach }}
    </ul>
</body>
</html>

